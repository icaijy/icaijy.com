Run the browser-logic regression tests with:

```bash
node --test brainrot/js_tests/run_analysis.test.mjs brainrot/js_tests/counter_prediction.test.mjs
```

These tests cover pure pacing-analysis and live score-projection helpers without requiring a browser.
